# OS / editor cruft
.DS_Store
*.swp
*~

# Logs and scratch
*.log
/tmp/
scratch/

# Local, machine-specific overrides (keep these out of the repo)
.qwen/sandbox.Dockerfile.local
settings/settings.local.json

# Language caches (in case examples are added later)
__pycache__/
node_modules/
